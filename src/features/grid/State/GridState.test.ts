import { GridStateUtil} from "./GridState"
describe('testing grid state',()=>{
    describe("get the hard version", ()=>{    
        it('selectSelectedCellIndexes returns selectedCells', ()=>{
/*             let result = GridStateUtil.getHardGrid()
            expect(result.length).toBe(81) */
        })
    })

})